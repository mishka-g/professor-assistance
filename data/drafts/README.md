# Drafts — student papers to review

Put the student `.docx` drafts to be reviewed here.

Review one with:

```
profa review data/drafts/<file>.docx
```

Output lands in `output/<file>/` as `reviewed.docx` (inline comments) and `suggestions.md` (redline + reasons).

This folder is git-ignored.
